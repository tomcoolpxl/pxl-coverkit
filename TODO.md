# TODO — Phase 1 (skeleton, data layer, Pages deploy)

Live work tracking for the current phase. Items move to `DONE.md` only after the verification gate passes for them.

## Outstanding (not yet verifiable in-tree)

- [ ] Manual walkthrough in a real browser: import a hand-crafted JSON with one fake card → settings shows the card count; export → file downloads with `schemaVersion`.
- [ ] Pages deploy succeeds: open the published URL after merging `phase-1-skeleton` to `main` and confirm it serves the same empty shell. (Requires the repo's Pages source to be set to "GitHub Actions" in the repo settings.)
